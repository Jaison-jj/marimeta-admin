'use strict';

/**
 * why-choose-marimeta router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::why-choose-marimeta.why-choose-marimeta');
