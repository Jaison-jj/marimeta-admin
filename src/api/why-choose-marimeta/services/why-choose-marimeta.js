'use strict';

/**
 * why-choose-marimeta service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::why-choose-marimeta.why-choose-marimeta');
